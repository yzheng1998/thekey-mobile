language: node_js

node_js:
  - '8'

os: osx
osx_image: xcode9.4

cache:
  - bundler

install:
  - npm install

after_install:
  - echo -e "machine github.com\n  login $CI_USER_TOKEN" >> ~/.netrc
  - cd ios
  - gem install bundler
  - bundle install

script:
  - bundle exec fastlane ios beta
