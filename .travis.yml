language: node_js

node_js:
  - '8'

os: osx
osx_image: xcode9.4

cache:
  - bundler

before_install:
  - echo -e "machine github.com\n  login $CI_USER_TOKEN" >> ~/.netrc

install:
  - npm install

script:
  - cd ios
  - gem install bundler
  - bundle install
  - bundle exec fastlane ios beta
